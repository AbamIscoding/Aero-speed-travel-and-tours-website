# Absolute trusted origin used to generate social-preview URLs.
SITE_URL=http://localhost:3000

# HTTPS endpoint that accepts trip-request JSON. Leave empty to keep the form in clearly labelled preview mode.
NEXT_PUBLIC_CONTACT_FORM_ENDPOINT=
